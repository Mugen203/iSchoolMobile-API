namespace iSchool_Solution.Features.Finance.DownloadInvoice;

public class Models
{
    // Request uses route parameter {invoiceId}
    // Response is typically the file itself (FileStreamResult, FileContentResult) handled by the endpoint logic, not a specific JSON model.

}